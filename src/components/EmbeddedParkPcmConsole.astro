<section class="park-pcm-shell" data-park-pcm data-authenticated="false" data-amap-key="8c2f9f3401e8d0ddfd619074c5f034ef">
  <header class="park-pcm-head">
    <div>
      <p class="section-kicker">PARK PEOPLE FLOW</p>
      <h1>园区人流采集</h1>
    </div>
    <div class="park-pcm-head-actions">
      <p class="park-pcm-status" data-park-pcm-status data-state="idle">初始化</p>
    </div>
  </header>

  <div class="park-pcm-auth" data-park-pcm-auth hidden></div>

  <section class="park-pcm-band park-pcm-vehicle-bar">
    <label class="park-pcm-vehicle-picker">
      <span>车辆</span>
      <select data-park-pcm-crowd-vehicle>
        <option value="">加载中...</option>
      </select>
    </label>
    <p class="park-pcm-vehicle-summary" data-park-pcm-vehicle-summary>选择车辆后显示该车的人流热力和现场图片。</p>
  </section>

  <section class="park-pcm-band park-pcm-map-band">
    <div class="park-pcm-band-head">
      <div>
        <p class="section-kicker">AMAP HEATMAP</p>
        <h2>人流热力图</h2>
      </div>
      <div class="park-pcm-head-actions">
        <p data-park-pcm-map-status>正在加载热力数据</p>
        <button type="button" class="park-pcm-button park-pcm-button--compact" data-park-pcm-patrol-refresh>刷新</button>
      </div>
    </div>
    <div class="park-pcm-map" data-park-pcm-map>
      <div class="park-pcm-map-fallback" data-park-pcm-map-fallback>高德地图加载中</div>
      <div class="park-pcm-heat-legend" data-park-pcm-heat-legend hidden>
        <span>人流热力</span>
        <i aria-hidden="true"></i>
        <span>高</span>
      </div>
    </div>
    <div class="park-pcm-date-control" data-park-pcm-heatmap-date hidden>
      <div class="park-pcm-date-head">
        <div>
          <span>日期热力</span>
          <strong data-park-pcm-heatmap-date-label>最新日期</strong>
        </div>
        <p data-park-pcm-heatmap-date-summary>按天查看热力图。</p>
      </div>
      <input
        type="range"
        min="0"
        max="0"
        step="1"
        value="0"
        aria-label="选择人流热力日期"
        data-park-pcm-heatmap-date-range
      />
      <div class="park-pcm-date-ticks" data-park-pcm-heatmap-date-ticks></div>
    </div>
    <div class="park-pcm-vehicle-detail" data-park-pcm-vehicle-detail>
      <p class="park-pcm-empty">选择车辆后显示人流统计。</p>
    </div>
    <div class="park-pcm-track-panel">
      <div class="park-pcm-track-list" data-park-pcm-track-samples>
        <p class="park-pcm-empty">等待人流记录。</p>
      </div>
      <div class="park-pcm-track-detail" data-park-pcm-track-detail>
        <p class="park-pcm-empty">选择热力记录查看图片。</p>
      </div>
    </div>
  </section>

</section>

<style is:global>
  .park-pcm-page {
    min-height: 100vh;
    padding: 112px 16px 56px;
    background:
      linear-gradient(135deg, rgba(8, 47, 73, 0.68), rgba(12, 25, 44, 0.72) 42%, rgba(7, 20, 18, 0.74)),
      url("/assets/fallback/hero-cloud-ops-1600.jpg") center / cover fixed;
  }

  .park-crowd-main {
    padding-top: 96px;
  }

  .park-crowd-card {
    overflow: visible;
  }

  .park-pcm-shell {
    width: 100%;
    margin: 0 auto;
    display: grid;
    gap: 18px;
    color: #e5edf8;
  }

  .park-pcm-shell > * {
    min-width: 0;
  }

  .park-pcm-head,
  .park-pcm-band,
  .park-pcm-auth {
    border: 1px solid rgba(148, 163, 184, 0.2);
    background: rgba(15, 23, 42, 0.54);
    box-shadow: none;
    backdrop-filter: none;
  }

  .park-pcm-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 18px;
    padding: 0 0 4px;
    border: 0;
    background: transparent;
    min-width: 0;
  }

  .park-pcm-head > div {
    min-width: 0;
  }

  .park-pcm-head h1,
  .park-pcm-band h2 {
    margin: 0;
    line-height: 1.2;
    letter-spacing: 0;
    overflow-wrap: anywhere;
  }

  .park-pcm-head h1 {
    font-size: clamp(1.75rem, 3vw, 2.45rem);
  }

  .park-pcm-band h2 {
    font-size: 1.2rem;
  }

  .park-pcm-head-actions {
    display: flex;
    align-items: center;
    justify-content: flex-end;
    gap: 10px;
    flex-wrap: wrap;
  }

  .park-pcm-status,
  .park-pcm-band-head p,
  .park-pcm-empty {
    margin: 0;
    color: #b8c7da;
    line-height: 1.6;
  }

  .park-pcm-status {
    min-width: 88px;
    text-align: right;
  }

  .park-pcm-status[data-state="ok"] {
    color: #6ee7b7;
  }

  .park-pcm-status[data-state="warn"] {
    color: #fbbf24;
  }

  .park-pcm-status[data-state="error"] {
    color: #fca5a5;
  }

  .park-pcm-button {
    min-height: 40px;
    border: 1px solid rgba(103, 232, 249, 0.38);
    background: rgba(8, 145, 178, 0.22);
    color: #e0faff;
    font-weight: 800;
    cursor: pointer;
    padding: 0 14px;
  }

  .park-pcm-button:disabled {
    cursor: not-allowed;
    opacity: 0.55;
  }

  .park-pcm-button--compact {
    min-height: 34px;
    padding: 0 12px;
  }

  .park-pcm-vehicle-bar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    flex-wrap: wrap;
  }

  .park-pcm-vehicle-picker {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    color: #dbeafe;
    font-weight: 800;
  }

  .park-pcm-vehicle-picker select {
    min-width: 220px;
    min-height: 40px;
    border: 1px solid rgba(148, 163, 184, 0.28);
    background: rgba(2, 6, 23, 0.52);
    color: #f8fafc;
    padding: 0 10px;
  }

  .park-pcm-vehicle-summary {
    margin: 0;
    color: #b8c7da;
    line-height: 1.5;
  }

  .park-pcm-layout {
    display: grid;
    grid-template-columns: minmax(360px, 0.78fr) minmax(0, 1.22fr);
    gap: 18px;
    align-items: stretch;
  }

  .park-pcm-patrol-summary {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 8px;
    margin-bottom: 12px;
  }

  .park-pcm-patrol-summary span {
    min-height: 38px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border: 1px solid rgba(148, 163, 184, 0.16);
    background: rgba(2, 6, 23, 0.34);
    color: #dbeafe;
    font-weight: 800;
  }

  .park-pcm-upload-bar {
    border-color: rgba(96, 165, 250, 0.24);
  }

  .park-pcm-band-head--compact {
    margin-bottom: 10px;
  }

  .park-pcm-upload-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 8px;
    margin-bottom: 10px;
  }

  .park-pcm-upload-grid span {
    min-height: 38px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0 8px;
    border: 1px solid rgba(148, 163, 184, 0.16);
    background: rgba(2, 6, 23, 0.34);
    color: #dbeafe;
    font-weight: 800;
    overflow-wrap: anywhere;
  }

  .park-pcm-collector-summary {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 8px;
    margin: 12px 0 10px;
  }

  .park-pcm-collector-summary span {
    min-height: 36px;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0 8px;
    border: 1px solid rgba(148, 163, 184, 0.16);
    background: rgba(2, 6, 23, 0.28);
    color: #dbeafe;
    font-weight: 800;
    overflow-wrap: anywhere;
  }

  .park-pcm-collector-list {
    display: grid;
    gap: 8px;
  }

  .park-pcm-collector-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 10px;
    align-items: center;
    padding: 10px 12px;
    border: 1px solid rgba(45, 212, 191, 0.18);
    background: rgba(15, 23, 42, 0.5);
  }

  .park-pcm-collector-row strong,
  .park-pcm-collector-row span {
    display: block;
    line-height: 1.45;
    overflow-wrap: anywhere;
  }

  .park-pcm-collector-row strong {
    color: #f8fafc;
  }

  .park-pcm-collector-row span {
    color: #9fb0c7;
  }

  .park-pcm-collector-row-actions {
    display: inline-flex;
    align-items: center;
    justify-content: flex-end;
    gap: 8px;
  }

  .park-pcm-upload-list {
    display: grid;
    gap: 8px;
  }

  .park-pcm-upload-row {
    display: grid;
    gap: 5px;
    padding: 10px 12px;
    border: 1px solid rgba(96, 165, 250, 0.2);
    background: rgba(15, 23, 42, 0.5);
  }

  .park-pcm-upload-row strong,
  .park-pcm-upload-row span {
    display: block;
    line-height: 1.45;
    overflow-wrap: anywhere;
  }

  .park-pcm-upload-row strong {
    color: #f8fafc;
  }

  .park-pcm-upload-row span {
    color: #9fb0c7;
  }

  .park-pcm-patrol-list {
    display: grid;
    gap: 8px;
    max-height: 438px;
    overflow: auto;
    padding-right: 4px;
  }

  .park-pcm-patrol-row {
    display: grid;
    gap: 6px;
    padding: 10px 12px;
    border: 1px solid rgba(45, 212, 191, 0.2);
    background: rgba(15, 23, 42, 0.55);
    cursor: pointer;
  }

  .park-pcm-patrol-row:focus-visible {
    outline: 2px solid rgba(103, 232, 249, 0.75);
    outline-offset: 2px;
  }

  .park-pcm-patrol-row strong {
    color: #f8fafc;
  }

  .park-pcm-patrol-row span {
    color: #9fb0c7;
    line-height: 1.5;
  }

  .park-pcm-map-band {
    min-height: 680px;
  }

  .park-pcm-map {
    position: relative;
    min-height: 520px;
    overflow: hidden;
    border: 1px solid rgba(148, 163, 184, 0.16);
    background: #07121f;
  }

  .park-pcm-map-fallback {
    position: absolute;
    inset: 0;
    display: grid;
    place-items: center;
    padding: 24px;
    color: #b8c7da;
    text-align: center;
    z-index: 1;
  }

  .park-pcm-map-fallback[hidden] {
    display: none;
  }

  .park-pcm-custom-heatmap {
    position: absolute;
    left: 0;
    top: 0;
    z-index: 3;
    display: block;
    width: 100%;
    height: 100%;
    opacity: 0.72;
    pointer-events: none;
  }

  .park-pcm-custom-heatmap[hidden] {
    display: none;
  }

  .park-pcm-heat-legend {
    position: absolute;
    left: 12px;
    bottom: 12px;
    z-index: 4;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 10px;
    border: 1px solid rgba(148, 163, 184, 0.22);
    background: rgba(2, 6, 23, 0.72);
    color: #e5edf8;
    font-size: 0.78rem;
    font-weight: 800;
    pointer-events: none;
  }

  .park-pcm-heat-legend[hidden] {
    display: none;
  }

  .park-pcm-heat-legend i {
    width: 88px;
    height: 10px;
    display: block;
    background: linear-gradient(90deg, rgba(34, 197, 94, 0.42), rgba(250, 204, 21, 0.62), rgba(251, 146, 60, 0.76), rgba(239, 68, 68, 0.86));
  }

  .park-pcm-date-control {
    display: grid;
    gap: 12px;
    margin-top: 12px;
    padding: 12px;
    border: 1px solid rgba(148, 163, 184, 0.16);
    background: rgba(15, 23, 42, 0.48);
    overflow-x: auto;
  }

  .park-pcm-date-control[hidden] {
    display: none;
  }

  .park-pcm-date-head {
    min-width: 0;
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 12px;
  }

  .park-pcm-date-head div {
    min-width: 0;
    display: grid;
    gap: 3px;
  }

  .park-pcm-date-head span,
  .park-pcm-date-head p {
    margin: 0;
    color: #94a3b8;
    font-size: 0.78rem;
    line-height: 1.35;
  }

  .park-pcm-date-head strong {
    color: #f8fafc;
    font-size: 1rem;
    line-height: 1.25;
    overflow-wrap: anywhere;
  }

  .park-pcm-date-head p {
    text-align: right;
    overflow-wrap: anywhere;
  }

  .park-pcm-date-control input[type="range"] {
    width: 100%;
    accent-color: #38bdf8;
    cursor: pointer;
  }

  .park-pcm-date-control input[type="range"]:disabled {
    cursor: not-allowed;
    opacity: 0.55;
  }

  .park-pcm-date-ticks {
    min-width: 0;
    display: grid;
    align-items: start;
    gap: 0;
  }

  .park-pcm-date-tick {
    min-width: 0;
    display: grid;
    justify-items: center;
    gap: 5px;
    color: #94a3b8;
    font-size: 0.72rem;
    line-height: 1.2;
    text-align: center;
  }

  .park-pcm-date-tick::before {
    content: "";
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: rgba(148, 163, 184, 0.5);
  }

  .park-pcm-date-tick[data-has-data="false"] {
    color: rgba(148, 163, 184, 0.56);
  }

  .park-pcm-date-tick[data-has-data="false"]::before {
    background: rgba(148, 163, 184, 0.24);
  }

  .park-pcm-date-tick[data-active="true"] {
    color: #f8fafc;
    font-weight: 800;
  }

  .park-pcm-date-tick[data-active="true"]::before {
    width: 10px;
    height: 10px;
    background: #fbbf24;
    box-shadow: 0 0 0 4px rgba(251, 191, 36, 0.16);
  }

  .park-pcm-vehicle-detail {
    display: grid;
    gap: 10px;
    margin-top: 12px;
  }

  .park-pcm-track-panel {
    display: grid;
    grid-template-columns: minmax(260px, 0.3fr) minmax(0, 0.7fr);
    gap: 10px;
    margin-top: 12px;
  }

  .park-pcm-track-list,
  .park-pcm-track-detail {
    min-width: 0;
    border: 1px solid rgba(148, 163, 184, 0.14);
    background: rgba(15, 23, 42, 0.48);
  }

  .park-pcm-track-list {
    display: grid;
    gap: 6px;
    max-height: 280px;
    overflow: auto;
    padding: 8px;
  }

  .park-pcm-track-detail {
    padding: 10px;
  }

  .park-pcm-track-point {
    width: 100%;
    min-height: 52px;
    border: 1px solid rgba(45, 212, 191, 0.16);
    background: rgba(2, 6, 23, 0.42);
    color: #dbeafe;
    text-align: left;
    cursor: pointer;
    padding: 8px 10px;
  }

  .park-pcm-track-point[data-active="true"] {
    border-color: rgba(251, 191, 36, 0.78);
    background: rgba(120, 53, 15, 0.34);
  }

  .park-pcm-track-point strong,
  .park-pcm-track-point span {
    display: block;
    line-height: 1.45;
    overflow-wrap: anywhere;
  }

  .park-pcm-track-point span {
    color: #9fb0c7;
    font-size: 0.82rem;
  }

  .park-pcm-track-head {
    display: grid;
    gap: 4px;
    margin-bottom: 10px;
  }

  .park-pcm-track-head strong {
    color: #f8fafc;
  }

  .park-pcm-track-head span {
    color: #9fb0c7;
    line-height: 1.5;
  }

  .park-pcm-track-frames {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 8px;
  }

  .park-pcm-detail-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 8px;
  }

  .park-pcm-detail-cell {
    min-width: 0;
    padding: 10px;
    border: 1px solid rgba(148, 163, 184, 0.14);
    background: rgba(15, 23, 42, 0.48);
  }

  .park-pcm-detail-cell span {
    display: block;
    color: #94a3b8;
    font-size: 0.78rem;
    line-height: 1.4;
  }

  .park-pcm-detail-cell strong {
    display: block;
    margin-top: 4px;
    color: #f8fafc;
    line-height: 1.35;
    overflow-wrap: anywhere;
  }

  .park-pcm-chart-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 10px;
  }

  .park-pcm-chart-card {
    min-width: 0;
    min-height: 184px;
    display: grid;
    grid-template-rows: auto minmax(0, 1fr);
    gap: 12px;
    padding: 12px;
    border: 1px solid rgba(148, 163, 184, 0.16);
    background: rgba(2, 6, 23, 0.4);
  }

  .park-pcm-chart-card--wide {
    grid-column: span 2;
  }

  .park-pcm-chart-card--trend {
    grid-column: 1 / -1;
    min-height: 292px;
  }

  .park-pcm-chart-head {
    min-width: 0;
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
  }

  .park-pcm-chart-head h3 {
    margin: 0;
    color: #f8fafc;
    font-size: 0.98rem;
    line-height: 1.25;
  }

  .park-pcm-chart-head span {
    flex: 0 1 auto;
    color: #94a3b8;
    font-size: 0.76rem;
    line-height: 1.3;
    text-align: right;
    overflow-wrap: anywhere;
  }

  .park-pcm-chart-body {
    min-width: 0;
    display: grid;
    grid-template-columns: 112px minmax(0, 1fr);
    gap: 12px;
    align-items: center;
  }

  .park-pcm-trend-layout {
    min-width: 0;
    display: grid;
    grid-template-columns: minmax(0, 1fr) 172px;
    gap: 12px;
    align-items: stretch;
  }

  .park-pcm-trend-plot {
    min-width: 0;
    min-height: 214px;
    display: grid;
    align-items: stretch;
    border: 1px solid rgba(148, 163, 184, 0.12);
    background:
      linear-gradient(180deg, rgba(14, 165, 233, 0.06), rgba(15, 23, 42, 0.08)),
      rgba(15, 23, 42, 0.22);
  }

  .park-pcm-trend-svg {
    width: 100%;
    height: 100%;
    min-height: 214px;
    display: block;
  }

  .park-pcm-trend-grid {
    stroke: rgba(148, 163, 184, 0.2);
    stroke-width: 1;
    vector-effect: non-scaling-stroke;
  }

  .park-pcm-trend-axis-label {
    fill: #94a3b8;
    font-size: 12px;
    font-weight: 700;
  }

  .park-pcm-trend-area {
    fill: rgba(56, 189, 248, 0.16);
  }

  .park-pcm-trend-line {
    fill: none;
    stroke: #38bdf8;
    stroke-width: 4;
    stroke-linecap: round;
    stroke-linejoin: round;
    vector-effect: non-scaling-stroke;
  }

  .park-pcm-trend-dot {
    fill: #e0f2fe;
    stroke: #0f172a;
    stroke-width: 2;
    vector-effect: non-scaling-stroke;
  }

  .park-pcm-trend-dot--peak {
    fill: #fbbf24;
    stroke: #7c2d12;
  }

  .park-pcm-trend-metrics {
    min-width: 0;
    display: grid;
    gap: 8px;
    align-content: stretch;
  }

  .park-pcm-trend-metric {
    min-width: 0;
    display: grid;
    gap: 3px;
    align-content: center;
    padding: 9px 10px;
    border-left: 3px solid rgba(56, 189, 248, 0.7);
    background: rgba(15, 23, 42, 0.34);
  }

  .park-pcm-trend-metric span {
    color: #94a3b8;
    font-size: 0.74rem;
    line-height: 1.2;
  }

  .park-pcm-trend-metric strong {
    color: #f8fafc;
    font-size: 1rem;
    line-height: 1.2;
    overflow-wrap: anywhere;
  }

  .park-pcm-trend-metric em {
    color: #9fb0c7;
    font-size: 0.72rem;
    font-style: normal;
    line-height: 1.25;
    overflow-wrap: anywhere;
  }

  .park-pcm-trend-empty {
    min-height: 214px;
    display: grid;
    place-items: center;
    padding: 16px;
    border: 1px dashed rgba(148, 163, 184, 0.22);
    text-align: center;
  }

  .park-pcm-donut {
    width: 112px;
    aspect-ratio: 1;
    display: grid;
    place-items: center;
    border-radius: 50%;
    background: var(--chart-fill);
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.08);
  }

  .park-pcm-donut-core {
    width: 68px;
    aspect-ratio: 1;
    display: grid;
    place-items: center;
    align-content: center;
    border-radius: 50%;
    background: rgba(2, 6, 23, 0.88);
    border: 1px solid rgba(148, 163, 184, 0.18);
  }

  .park-pcm-donut-core strong {
    color: #f8fafc;
    font-size: 1.15rem;
    line-height: 1;
  }

  .park-pcm-donut-core span {
    margin-top: 4px;
    color: #94a3b8;
    font-size: 0.72rem;
    line-height: 1;
  }

  .park-pcm-chart-legend,
  .park-pcm-risk-list {
    min-width: 0;
    display: grid;
    gap: 7px;
  }

  .park-pcm-chart-row,
  .park-pcm-risk-row {
    position: relative;
    min-width: 0;
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 8px;
    align-items: center;
    padding-bottom: 7px;
  }

  .park-pcm-chart-row span {
    min-width: 0;
    display: inline-flex;
    align-items: center;
    gap: 7px;
    color: #cbd5e1;
    font-size: 0.8rem;
    line-height: 1.25;
  }

  .park-pcm-chart-row i {
    width: 9px;
    height: 9px;
    flex: 0 0 auto;
    border-radius: 50%;
  }

  .park-pcm-chart-row em {
    min-width: 0;
    font-style: normal;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .park-pcm-chart-row strong,
  .park-pcm-risk-row strong {
    color: #e5edf8;
    font-size: 0.78rem;
    line-height: 1.2;
  }

  .park-pcm-chart-row b,
  .park-pcm-risk-row b {
    position: absolute;
    left: 0;
    bottom: 0;
    height: 3px;
    border-radius: 999px;
    background: rgba(56, 189, 248, 0.72);
  }

  .park-pcm-risk-row {
    min-height: 31px;
    padding: 0 0 8px;
  }

  .park-pcm-risk-row span {
    min-width: 0;
    color: #cbd5e1;
    font-size: 0.82rem;
    line-height: 1.3;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .park-pcm-chart-empty {
    margin: 0;
    color: #94a3b8;
    font-size: 0.82rem;
    line-height: 1.5;
  }

  .park-pcm-detail-note {
    margin: 0;
    color: #9fb0c7;
    line-height: 1.55;
  }

  .park-pcm-crowd-demo {
    border-color: rgba(45, 212, 191, 0.24);
  }

  .park-pcm-crowd-controls {
    display: flex;
    align-items: center;
    gap: 12px;
    flex-wrap: wrap;
    margin-bottom: 14px;
  }

  .park-pcm-crowd-controls label {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: #cbd5e1;
    font-weight: 800;
  }

  .park-pcm-crowd-controls select,
  .park-pcm-crowd-controls input {
    min-height: 38px;
    border: 1px solid rgba(148, 163, 184, 0.28);
    background: rgba(2, 6, 23, 0.52);
    color: #f8fafc;
    padding: 0 10px;
  }

  .park-pcm-crowd-controls select {
    min-width: 160px;
  }

  .park-pcm-crowd-controls input {
    width: 86px;
  }

  .park-pcm-crowd-last {
    display: grid;
    gap: 6px;
    margin-bottom: 14px;
    padding: 12px;
    border: 1px solid rgba(148, 163, 184, 0.14);
    background: rgba(15, 23, 42, 0.5);
  }

  .park-pcm-crowd-last strong {
    color: #f8fafc;
  }

  .park-pcm-crowd-last span {
    color: #9fb0c7;
  }

  .park-pcm-crowd-gallery {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 10px;
  }

  .park-pcm-crowd-frame {
    min-width: 0;
    border: 1px solid rgba(148, 163, 184, 0.16);
    background: rgba(2, 6, 23, 0.46);
  }

  .park-pcm-frame-preview-button {
    display: block;
    width: 100%;
    padding: 0;
    border: 0;
    background: transparent;
    cursor: zoom-in;
  }

  .park-pcm-frame-preview-button:focus-visible {
    outline: 2px solid #38bdf8;
    outline-offset: 2px;
  }

  .park-pcm-crowd-frame img {
    display: block;
    width: 100%;
    aspect-ratio: 4 / 3;
    object-fit: cover;
    background: #020617;
  }

  .park-pcm-crowd-frame figcaption {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    padding: 8px;
    color: #cbd5e1;
    font-size: 0.82rem;
  }

  .park-pcm-image-preview {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: grid;
    place-items: center;
    padding: 22px;
    background: rgba(2, 6, 23, 0.88);
  }

  .park-pcm-image-preview[hidden] {
    display: none;
  }

  .park-pcm-image-preview-frame {
    width: min(1120px, 96vw);
    max-height: 92vh;
    display: grid;
    grid-template-rows: auto minmax(0, 1fr);
    border: 1px solid rgba(148, 163, 184, 0.26);
    background: #020617;
  }

  .park-pcm-image-preview-toolbar {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    padding: 10px 12px;
    border-bottom: 1px solid rgba(148, 163, 184, 0.18);
    color: #e5edf8;
  }

  .park-pcm-image-preview-toolbar div {
    min-width: 0;
    display: grid;
    gap: 2px;
  }

  .park-pcm-image-preview-toolbar strong,
  .park-pcm-image-preview-toolbar span {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .park-pcm-image-preview-toolbar span {
    color: #9fb0c7;
    font-size: 0.86rem;
  }

  .park-pcm-image-preview-close {
    width: 36px;
    height: 36px;
    flex: 0 0 auto;
    border: 1px solid rgba(148, 163, 184, 0.28);
    background: rgba(15, 23, 42, 0.9);
    color: #e5edf8;
    font-size: 1.5rem;
    line-height: 1;
    cursor: pointer;
  }

  .park-pcm-image-preview-close:hover {
    background: rgba(30, 41, 59, 0.95);
  }

  .park-pcm-image-preview img {
    display: block;
    width: 100%;
    max-height: calc(92vh - 58px);
    object-fit: contain;
    background: #020617;
  }

  .park-pcm-auth {
    padding: 18px;
    color: #fecaca;
  }

  .park-pcm-band {
    padding: 18px;
  }

  .park-pcm-band-head {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 16px;
    margin-bottom: 14px;
  }

  @media (max-width: 860px) {
    .park-pcm-page {
      padding-top: 88px;
    }

    .park-pcm-head,
    .park-pcm-band-head {
      align-items: stretch;
      flex-direction: column;
    }

    .park-pcm-head-actions {
      justify-content: flex-start;
    }

    .park-pcm-status {
      width: 100%;
      text-align: left;
    }

    .park-pcm-layout {
      grid-template-columns: 1fr;
    }

    .park-pcm-map-band {
      min-height: 520px;
    }

    .park-pcm-map {
      min-height: 430px;
    }

    .park-pcm-track-panel {
      grid-template-columns: 1fr;
    }

    .park-pcm-date-head {
      align-items: stretch;
      flex-direction: column;
    }

    .park-pcm-date-head p {
      text-align: left;
    }

    .park-pcm-detail-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .park-pcm-chart-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .park-pcm-trend-layout {
      grid-template-columns: 1fr;
    }

    .park-pcm-trend-metrics {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }

    .park-pcm-upload-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .park-pcm-collector-summary {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .park-pcm-collector-row {
      grid-template-columns: 1fr;
    }

    .park-pcm-collector-row-actions {
      justify-content: flex-start;
    }

    .park-pcm-crowd-gallery {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 560px) {
    .park-pcm-detail-grid {
      grid-template-columns: 1fr;
    }

    .park-pcm-chart-grid {
      grid-template-columns: 1fr;
    }

    .park-pcm-chart-card--wide {
      grid-column: span 1;
    }

    .park-pcm-chart-card--trend {
      min-height: 0;
    }

    .park-pcm-chart-body {
      grid-template-columns: 96px minmax(0, 1fr);
    }

    .park-pcm-trend-plot,
    .park-pcm-trend-svg,
    .park-pcm-trend-empty {
      min-height: 190px;
    }

    .park-pcm-trend-metrics {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .park-pcm-donut {
      width: 96px;
    }

    .park-pcm-donut-core {
      width: 60px;
    }

    .park-pcm-upload-grid {
      grid-template-columns: 1fr;
    }

    .park-pcm-collector-summary {
      grid-template-columns: 1fr;
    }

    .park-pcm-track-frames {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .park-pcm-crowd-gallery {
      grid-template-columns: 1fr;
    }
  }
</style>
